ref:
  as: upi-conf-vsphere-ipam
  from: upi-installer
  commands: upi-conf-vsphere-ipam-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 128Mi
  documentation: >-
    vSphere OVA checks if appropriate template exists.
    If it does not exist download the ova and upload to vCenter.

